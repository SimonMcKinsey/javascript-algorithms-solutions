/**
    Given an array of unsorted numbers, find all unique triplets in it that add up to zero.
 */

function search_triplets(arr) {
  const triplets = [];

  return triplets;
}

const result = search_triplets([-3, 0, 1, 2, -1, 1, -2]);

console.log(result); // [-3, 1, 2], [-2, 0, 2], [-2, 1, 1], [-1, 0, 1]
